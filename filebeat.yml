filebeat.inputs:
  - type: log
    enabled: true
    paths:
      - /logs/*.log
    json.keys_under_root: true
    json.overwrite_keys: true
    json.add_error_key: true

output.kafka:
  hosts: ["kafka:9092"]
  topic: "app-logs"
  codec.json:
    pretty: false